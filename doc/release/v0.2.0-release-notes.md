# v0.2.0 Release Notes

**Status: local candidate; unreleased.** Public latest is `v0.1.0`; the local
target is `v0.2.0`.

## Local changes

The `[Unreleased]` changelog records local gateway work: opt-in gateway
authorization and rate/quota controls; resilience detection and attribute
coalescing; tamper-evident audit with data-plane gap reporting; startup backend
attestation; transparent-forwarding fixes for NULL/data forwarding and bounds,
mechanism-payload safety, Verify/Digest init forwarding, and close-attempt
session-cache eviction;
packaging mirrors for Alpine APK, Amazon Linux RPM, and CI; and workspace MSRV
`1.88`. Cargo workspace metadata is authoritative; APK/RPM/CI literals are
checked mirrors. This work is implemented locally, partially covered, and
unreleased; local unit and integration coverage is not a transparency matrix.

## Evidenced platform scope

The v0.2.0 tail stretch ([ADR-0014](../adr/ADR-0014-v020-tail-platform-stretch.md),
Implemented 2026-09-17) evidences the following; each claim traces to a named
receipt:

- Windows x64/MSVC native daemon consuming Windows provider DLLs, and the
  Windows x64 PKCS#11 client shim, in both interoperation directions — passed
  on real Windows Server 2022 (workspace-root
  `artifacts/v020-tail-windows-2026-09-16/`: leg A, Windows daemon +
  SoftHSM2-win DLL over mTLS driven by a Linux client; leg B, Windows shim
  DLL + smoke client vs a Linux daemon; leg C, BouncyHsm-win second provider,
  full set green). The Windows daemon is mTLS-over-TCP only: its config omits
  `[listener.local]` (leg-A config) and the daemon exits 1 with an mTLS
  pointer when it is present (leg-B negative receipt).
- 32-bit/mixed width claim — the four Linux topologies run as loaded-shim
  legs (`scripts/run-cross-width-live-test.sh` legs 1–4: 32c/64b, 64/64,
  64c/32b, 32/32) with a second 32-bit provider leg
  (`scripts/run-cross-width-nss32-live-test.sh`, NSS i386 softokn); nightly
  runs both via `scripts/run-test-tiers.sh live` with extracted i386 provider
  closures.
- Windows compile gate — per-PR Tier 0f `windows-client-llp64` job
  (`cargo xwin build --target x86_64-pc-windows-msvc --all-targets`).
- Deterministic Windows bundle — `scripts/release-windows.sh` produces
  `pkcs11-proxy-ng-v0.2.0-x86_64-pc-windows-msvc.zip` + `SHA256SUMS-windows`,
  appended to the tag release by the `release-windows` job's second softprops
  step; the T6 receipt set re-staged the bundle byte-identically.
- Windows abnormal-stop contract — `TerminateProcess(GetCurrentProcess(), 70)`
  backstop arm on the qualified Windows host, documented in the
  [native ownership contract](native-mechanism-ownership.md) (Windows
  abnormal-stop contract section); `abort()` ruled out.

Remaining platform exclusions: Windows GNU, 32-bit Windows (PE32), and
macOS/ARM runtime claims. Big-endian is proven one tier below a runtime
claim — s390x build plus the QEMU suites in
[be-qemu-tier.md](be-qemu-tier.md) are green; live native FFI on BE
hosts stays excluded. Wine is a dev-only smoke, never conformance
evidence.

## Current limitations

- The selected [native ownership contract](native-mechanism-ownership.md)
  remains unimplemented/unqualified: one managed provider chain per process,
  constructor reservation before loading/discovery, explicit native retirement,
  and a qualified raw Linux `exit_group(70)` last-owner/shutdown backstop.
  Direct embedders share one Arc and accept whole-process failure; independent
  chains require separate processes. Multiple backend runtime copies, unmanaged
  native access and downstream aggregator aliasing are excluded.
- v0.2 slot-event scope is `CKF_DONT_BLOCK` only. Blocking mode is locally
  FUNCTION_NOT_SUPPORTED without polling. Clients share native pending-event
  flags; logical Initialize creates no independent event bitmap. Checked widths,
  error precedence and the one ordinary-lifecycle waiter remain release gates.
- Live FFI is qualified on Linux GNU/musl x86_64/64-bit and x86/32-bit, and on
  Windows x64/MSVC per the evidenced scope above; nonqualified hosts must still
  refuse FFI construction before native loading. Still excluded: Windows GNU,
  32-bit Windows (PE32), and macOS/ARM runtime claims. Big-endian is proven
  one tier below a runtime claim (s390x build + QEMU suites green, no live
  BE FFI); see [be-qemu-tier.md](be-qemu-tier.md).
- The selected abnormal stop promises no wiping, complete audit tail, provider
  cleanup, token deletion, strict disappearance deadline or global core-dump
  suppression; its exact target/seccomp/environment limits are documented in
  the native ownership contract and [privacy contract](privacy.md).

- Denial is not constant-latency.
- Diagnostic bundles are private, allowlisted metadata snapshots. They exclude
  raw logs, configuration files, environment values, and command errors, and
  use descriptor-relative, create-only Python standard-library archive output
  rather than external archive tools. They must still be reviewed before
  sharing. Arbitrary-log sanitization is not a supported promise.
- Attestation runs at startup only; it has no config hash and no reconnect or
  hot-swap re-attestation.
- Audit contains operational metadata, never PINs, key material, or raw request
  payloads.
- Data-plane audit can drop records and reports gaps.
- Fail-closed audit can occur after a backend side effect.
- `EventClass::Deny` remains reserved rather than emitted.

## Release blockers

- Independently review this P0 contract amendment, implement the complete
  native owner/lifecycle/width/platform/stop design and obtain same-source
  native GNU/musl stop and all four Linux loaded-shim topology receipts.
- Preserve the independently accepted C3 Phase A corrections; complete C3M, the
  held classic-GCM Phase B work and later lifecycle/privacy/budget integrations.
  Contract adoption alone does not authorize or accept those remaining changes.

- Record the intended check commit and clean framework/test-data provenance.
- Produce dated direct, proxied, and comparison evidence for SoftHSM2, NSS
  softokn, and Kryoptic.
- Produce an explicit 30-provider non-mock matrix.
- Pass the standalone-clone gate.
- Obtain separate approval to tag, push, and publish.

## Explicit exclusions

This candidate has no Observatory, recorder or payload capture, target replay,
cassette playback, live mirror, or public `v0.2.0` artifact.
