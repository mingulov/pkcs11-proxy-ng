[package]
name = "pkcs11-proxy-ng-types"
version = "0.1.0"
edition.workspace = true

[dependencies]
serde = { workspace = true }
toml = { workspace = true }
tracing = { workspace = true }

[dev-dependencies]
tempfile = "3"
